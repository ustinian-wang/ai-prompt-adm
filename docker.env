# Docker环境变量配置
NODE_ENV=production
PORT=54321

# 数据库配置
DB_HOST=mysql
DB_PORT=3306
DB_USER=root
DB_PASSWORD=fai@508
DB_NAME=ai_prompt_admin

# JWT配置
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=7d

# 文件上传配置
UPLOAD_MAX_SIZE=100MB
UPLOAD_PATH=/app/uploads

# 静态文件访问基础域名（用于拼接OSS返回URL）
ASSET_BASE_URL=http://42.193.243.30:4002

# 日志配置
LOG_LEVEL=info
LOG_PATH=/app/data/logs

# 缓存配置
CACHE_TTL=3600
CACHE_PATH=/app/data/cache
