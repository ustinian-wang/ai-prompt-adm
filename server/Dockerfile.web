# 后端项目Dockerfile
FROM node:18-alpine

# 设置工作目录
WORKDIR /app

# 复制package文件
COPY package*.json ./
COPY yarn.lock ./

# 安装依赖
RUN yarn install --frozen-lockfile --production

# 复制源代码
COPY . .

# 创建必要的目录
RUN mkdir -p uploads logs


# 暴露端口
EXPOSE 4002

# 启动服务（日志自动输出到 stdout/stderr）
CMD [ "node", "src/app.js" ]
